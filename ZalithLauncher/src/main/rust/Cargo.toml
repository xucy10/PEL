[package]
name = "zalith-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
jni = { version = "0.21", features = ["invocation"] }
libc = "0.2"
log = "0.4"

[profile.release]
lto = true
opt-level = "s"
strip = true
panic = "abort"